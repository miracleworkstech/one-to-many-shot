---
target: app/page.tsx
total_score: 27
max_score: 40
na_heuristics: 
p0_count: 1
p1_count: 2
target_identity: "file:C:\\Users\\mirac\\Documents\\lumalabs-eng-take-home-13ed13959e5ba3e7acc72a8a9f41ecd409620c80\\app\\page.tsx"
target_fingerprint: "sha256:2d33ebaab3305a9766bd02fd8dd8fd7766fc40255f748c16432a8379eded8160"
target_path: "C:\\Users\\mirac\\Documents\\lumalabs-eng-take-home-13ed13959e5ba3e7acc72a8a9f41ecd409620c80\\app\\page.tsx"
timestamp: 2026-09-05T17-29-05Z
slug: app-page-tsx
---
Method: dual-agent (A: design-review subagent · B: detector+browser subagent)

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | HG-005 has "1 to decide" but is filed under Generating; header says 1 waiting, page holds 2 |
| 2 | Match System / Real World | 4 | Team words throughout; "Needs more" underspecified |
| 3 | User Control and Freedom | 2 | No cancel once queued; import has no preview |
| 4 | Consistency and Standards | 3 | Two filled primaries (Download, Generate); three styles for three export/import controls |
| 5 | Error Prevention | 2 | No cap in view before the money tap; count input refused only after |
| 6 | Recognition Rather Than Recall | 3 | Spend summary line carries no number |
| 7 | Flexibility and Efficiency | 2 | No search/filter/sort; "Show 288 more" at the 300 ceiling |
| 8 | Aesthetic and Minimalist Design | 3 | Seven identical section headers; uniformity, not hierarchy |
| 9 | Error Recovery | 3 | Failed rows carry no reason on this page |
| 10 | Help and Documentation | 2 | One helper sentence; nothing says what Done means |
| **Total** | | **27/40** | **Acceptable** |

## Design Specificity Verdict

LLM assessment: authored in content and IA (group order by who must act, spend as prose, dot-beside-text), near-generic in visual form. Squinted, it is the default quiet-Tailwind internal tool: stone ground, hairlines, one black button, the same chevron-dot-label-count header seven times. Specificity is carried by words and order only.

Deterministic scan: 0 findings (exit 0) over app/page.tsx and the three components. Overlay at 1280×900: 1 finding, ~90 chars/line on the Spend paragraph. One background-tab text-overflow hit discarded as a false positive (innerWidth 0). Typography 4 sizes / 3 weights, 5 text colours, no target under 44 px (the 1×1 sr-only file input is not a target). Page height 1084 px desktop, 1188 px at 375×812; first queue heading at y=241 on mobile, Generate at 583, Spend at 1096.

## Overall Impression

The content model is right and the visual form does none of the work. Three elements compete to be primary (Download, Generate, the queue) and the founder's sentence is a caption. The single biggest opportunity: make the page's shape say who has to act.

## What's Working

- Reading order by obligation (in_review, needs_more, then the trigger, then the passive states), not by enum.
- Spend as a paragraph, total first, with cost per approved image and approval rate: the anti-reference avoided.
- "Existing approvals are kept." under the import control, exactly where the fear is.

## Priority Issues

- [P0] Ellie's queue undercounts: `productStatus` ranks generating above in_review, so a product with a decidable candidate and a second batch in flight (HG-005) files under Generating and is missing from the header count. Fix: tier 1 = products with toDecide > 0 regardless of lifecycle status; header follows. Command: /impeccable harden
- [P1] No single primary; the status sentence is subordinate to the third user's button. Two filled buttons break the one-primary-per-state rule. Fix: status sentence at 20/600 as the hero; one primary derived from page state. Command: /impeccable layout
- [P1] Seven identical section headers; tiers exist only in source order (600 vs 500 at the same 16 px is invisible on a phone). Fix: tier 1 is a plain section, not a disclosure; passive states fold with counts; one visible break between "needs you" and "needs nobody". Command: /impeccable layout
- [P2] Action row: three styles for three controls, meaning split (the two exports separated by the import), helper line attached to the wrong gap on desktop. Fix: cluster by meaning. Command: /impeccable layout
- [P2] The Spend summary line says nothing; batches not newest-first; a 0-image $0.00 batch row; three identical "5 Sep" rows. Command: /impeccable clarify
- [P3] Rows have no affordance beyond text and hairline; Generate form wraps mid-sentence at 375 px. Command: /impeccable polish, /impeccable adapt

## Cognitive Load

4 of 8 fail: single focus, chunking (nine peers at one level), visual hierarchy, minimal choices (eight targets above the mobile fold).

## Persona Red Flags

- Casey: queue starts ~500 px down under three buttons that are not hers; rows look static; Generate form breaks across two lines.
- Sam: heading names read "Waiting for review1"; the disabled "No approved images yet" button is unfocusable; row link name leads with the SKU.
- Alex: no search/filter/sort; no "review next" jump; batch list capped at five with no route to the rest.
- Maya: status is a caption, spend is a closed word at the bottom; no last-updated cue.
- Ellie: HG-005 hidden under Generating; her queue can be collapsed by a mis-tap; "Needs more" does not say what it needs from her.

## Minor Observations

- Tab title "Styled Shots" vs h1 "Styled shots".
- Up-chevron on open groups, down-chevron on the nested "Show the 33 products" three lines below.
- Failed rows carry no reason; "Nothing here." never renders (dead copy).
- Desktop reads small, not spacious: phone type sizes in a 672 px measure.

## Questions to Consider

- Why is the reviewer's queue a disclosure at all? What if tier 1 were simply the page and everything else the disclosure?
- Why does the third, occasional user's button own the page's only black fill?
- Is Spend being hidden from Maya, or Maya protected from Spend? Is Done an outbox, and should Download live on it?
