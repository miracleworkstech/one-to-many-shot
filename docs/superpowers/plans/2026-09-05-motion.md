# Task 14 — Motion (D24)

Agreed 2026-09-05 from `/impeccable animate` across both pages, after PR #12 merged. The
motion thesis: feedback, state and continuity explained, one authored moment, nothing that
decorates. All CSS, transform and opacity only, reduced motion keeps opacity and drops
movement. Branch `task/14-motion`, one PR.

- [x] Sheets rise and fade in with their backdrop (`@starting-style` on the native popover),
      leave faster: `.sheet`, `.lightbox` in `app/globals.css`.
- [x] Disclosure contents unfold where the browser can animate `height: auto`
      (`interpolate-size`, `::details-content`), instant elsewhere.
- [x] Answers under a form arrive with a 150 ms fade: `.arrive` on every `role="status"`
      result (GenerateForm, GenerateProductForm, ImportForm, IdeaForm).
- [x] The approval moment: `.approved-in` on the Approved marker (dot lands, text fades),
      `.draw` on the end card's check (stroke draws over 400 ms).
- [x] Cross-document `@view-transition` for a 200 ms cross-fade on every full navigation;
      `next/link` replaced by plain anchors on both pages.
- [x] Contract amended (motion line), D24, STATE.
- [ ] Browser round at 375×812 and desktop, reduced-motion check → evaluator → Codex → PR →
      stop for review. Then `/impeccable document` writes DESIGN.md with the motion in it.

## Not doing

The drop bar growing on load; hover lifts; staggered lists; skeletons; anything on the
Generate or Download buttons beyond their pending text.
