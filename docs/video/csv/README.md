# Video CSV slices

Both files are `data/catalog.csv` split in order, rows unchanged. Same nine columns, CRLF.

| File | Rows | Sheet ideas | Use |
|---|---|---|---|
| `1-before-recording-20.csv` | HG-001 to HG-022 | 10, plus the "do this one first" note on HG-002 | Imported before recording. Generate a round, review a few on the phone, so the status page has history. |
| `2-live-import-20.csv` | HG-024 to HG-045 | 6, so 14 rows get a suggested idea | Imported on camera at 0:20. The two-product round comes from these. |

Order of the day: wipe the volume, deploy, import 1, generate a round, decide a few, then record with 2.
